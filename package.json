{
  "name": "sympatheticoutlandishpacket",
  "workspaces": [
    "{packages,apps}/**"
  ],
  "private": true,
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "4.3.0",
    "@types/bun": "latest",
    "@types/prettier": "^3.0.0",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "dev": "bun ./codepark.ts run --mode=dev",
    "build": "./codepark.shell.ts build",
    "clean": "rm -rf ./.codepark ./node_modules"
  }
}
